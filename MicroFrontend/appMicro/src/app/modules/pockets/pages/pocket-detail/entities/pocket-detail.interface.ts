export interface PocketDetailPayload {
  pocketId: string;
  pocketType: string;
  parentId: string;
  parentIdType: string;
}
export interface PocketWithReturnsDetailPayload {
  parentId: string;
  parentIdType: string;
  pocketId: string;
  pocketType: string;
}
