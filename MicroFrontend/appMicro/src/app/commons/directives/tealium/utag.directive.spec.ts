import { UtagDirective } from './utag.directive';

describe('UtagDirective', () => {
  it('should create an instance', () => {
    const directive = new UtagDirective();
    expect(directive).toBeTruthy();
  });
});
